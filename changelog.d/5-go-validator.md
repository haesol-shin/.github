## Added
- Add a fixture-only Go `repo-ops-validator` CLI and a checked-in Python/Go semantic differential harness with frozen intent and plan digest vectors.

## Changed
- Keep production repository-policy evaluation on Python while Stage 1 `--event` reports unsupported without live GitHub or git collection.
