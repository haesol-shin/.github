## Added
- Publish the first immutable repository-operations contract bundle with versioned schemas, a reusable policy workflow, and language-independent conformance fixtures.
- Add a trusted fragment lifecycle that validates pull-request declarations and deterministically prepares release changelogs.

## Changed
- Let consumers pin workflow code by full commit SHA while published schema identifiers resolve through the immutable release tag.
- Support non-closing `Related #N` issue authority alongside the existing closing links.
