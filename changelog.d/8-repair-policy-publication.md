## Fixed
- Load the repository-policy composite action by quoting the invalid `inputs.check` description scalar.

## Changed
- Publish exact-head `contract` and `merge approval` results from one internal `publish` job, select machine context `quality`, treat generic Issue and pull-request headings as advisory, and drop merge-review round counters.
