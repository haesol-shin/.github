[project]
name = "repo-ops-validator"
version = "0.1.0"
description = "Trusted repository policy and receipt validator"
requires-python = ">=3.12,<3.13"
dependencies = [
  "jsonschema==4.25.1",
  "pyyaml==6.0.2",
]

[tool.uv]
package = false
